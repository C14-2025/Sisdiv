[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sisdiv"            
version = "0.1.0"
description = "Projeto C14"
readme = "README.md"
authors = [
  { name = "test", email = "test@test" }
]
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = [
]
